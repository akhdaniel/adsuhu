<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="vit_md_viewer.MarkdownViewerField" owl="1">
        <div t-ref="root">
            <div class="o_markdown_viewer_toolbar">
                <button
                    type="button"
                    class="btn btn-link o_markdown_viewer_toggle"
                    t-on-click="toggleMode"
                    t-att-title="toggleTitle"
                    t-att-aria-label="toggleTitle"
                    t-att-disabled="props.readonly ? true : undefined"
                >
                    <i t-att-class="toggleIconClass" aria-hidden="true"/>
                </button>
                <!-- <button
                    type="button"
                    class="btn btn-link o_markdown_viewer_reformat"
                    t-on-click="reformatOutput"
                    t-att-title="reformatTitle"
                    t-att-aria-label="reformatTitle"
                    t-att-disabled="reformatDisabled ? true : undefined"
                >
                    <i t-att-class="reformatIconClass" aria-hidden="true"/>
                </button> -->
                <span class="o_markdown_viewer_copy_wrapper">
                    <button
                        type="button"
                        class="btn btn-link o_markdown_viewer_copy"
                        t-on-click="copyMarkdown"
                        t-att-title="copyButtonTitle"
                        t-att-aria-label="copyButtonTitle"
                        t-att-disabled="copyButtonDisabled ? true : undefined"
                    >
                        <i class="fa fa-copy" aria-hidden="true"/>
                    </button>
                    <t t-if="state.copyStatus === 'success'">
                        <div class="o_markdown_viewer_copy_popup">
                            <t t-esc="copyPopupText"/>
                        </div>
                    </t>
                </span>
            </div>
            <t t-if="shouldShowPreview">
                <div class="o_markdown_viewer_content" t-raw="renderedMarkdown"/>
            </t>
            <t t-else="">
                <!-- <div class="o_markdown_viewer_editor"> -->
                    <t t-call="web.TextField"/>
                <!-- </div> -->
                <!-- <div t-ref="div">
                    <textarea
                        class="o_input"
                        t-att-class="{
                            'o_field_translate': isTranslatable,
                            'o_field_placeholder': props.dynamicPlaceholder,
                        }"
                        t-att-id="props.id"
                        t-att-placeholder="props.placeholder"
                        t-att-rows="rowCount"
                        t-ref="textarea"
                        t-on-blur="onBlur"
                    />
                </div> -->
            </t>
        </div>
    </t>
</templates>
